/**
 * @typedef Settings
 * @prop {string} commentReplyMode
 * @prop {number} eyes
 * @prop {string} postMode
 * @prop {number} siteWidth
 * @prop {string} timezone
 */

// Stub is needed for jsdoc to work
export {};
